machine:

  python:
    varsion: 2.7.9

  services:
    - docker

dependencies:
  override:
    - docker info
    - pip install -U -r ansible/roles/machine/files/requirements.txt


test:
  override:
    - cd ansible
    - ansible-playbook roles/openresty/tests/docker.yml
