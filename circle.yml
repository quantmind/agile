machine:

  python:
    version: 2.7.10

  services:
    - docker

dependencies:
  override:
    - docker info
    - pip install -U pip
    - pip install -U -r ansible/roles/machine/files/requirements.txt


test:
  override:
    - cd ansible; ansible-playbook roles/openresty/tests/docker.yml
