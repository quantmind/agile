{
    "python": {
        "agile_version": "agile.__version__"
    },
    "github": {
        "release": {
            "description": "release pulsar-agile to github",
            "version": "{{ agile_version }}",
            "release_notes": "docs/history"
        }
    },
    "shell": {
        "clean": {
            "command": ["rm -rf build",
                        "rm -rf dist",
                        "rm -rf *.log",
                        "rm -rf *.egg-info"]
        }
    }
}
