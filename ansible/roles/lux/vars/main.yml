#
# service home path
service_home_path: "{{ lux_service_path }}/{{ service_user }}"
#
# Service stage path
service_running_path: "{{ service_home }}/running"

# Log directory
service_log_path: "{{ lux_log_path }}/{{ service_user }}"
